<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="/stylesheets/style.css" />

</head>
<%-include('../partials/_navbar.ejs') %>

<body>

  <h1>Welcome <%= user.name %> to your recipes!</h1>

  <ul>
    <% recipes.forEach((item)=>{ %>
    <a href="/users/<%= user._id %>/recipes/<%= item._id %>">
      <li><%= item.name %></li>
    </a>
    <% }) %>
  </ul>

  <a href="/users/<%= user._id %>/recipes/new"><button>Add new</button></a>

</body>

</html>