<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<%-include('../partials/_navbar.ejs') %>

<body>

  <h1>Welcome <%= user.name %> to your ingredients!</h1>

  <ul>
    <% ingredients.forEach((item)=>{ %>
    <a href="/users/<%= user._id %>/ingredients/<%= item._id %>">
      <li><%= item.name %></li>
    </a>
    <% }) %>
  </ul>

  <a href="/users/<%= user._id %>/ingredients/new"><button>Add new</button></a>

</body>

</html>